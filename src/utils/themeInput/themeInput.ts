import { createTheme } from '@mui/material/styles';

export const themeInput = createTheme({
  palette: {
    secondary: {
      main: '#000000',
    },
    success: {
      main: '#4caf50',
    },
    warning: {
      main: '#931919',
    },
  },
});
